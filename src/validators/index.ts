/**
 * Validators directory
 * Add your validation schemas here using express-validator
 *
 * Example:
 * export const createUserValidation = [
 *   body('email').isEmail().normalizeEmail(),
 *   body('password').isLength({ min: 8 }),
 *   body('name').trim().notEmpty(),
 * ];
 */

export {};
